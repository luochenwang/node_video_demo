<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,Chrome=1" />
    <meta name="format-detection" content="telphone=no" />
    <meta name="format-detection" content="email=no" />
    <meta http-equiv="Expires" content="0" />
    <meta http-equiv="Pragma" content="no-cache" />
    <meta http-equiv="Cache-control" content="no-cache" />
    <meta http-equiv="Cache" content="no-cache" />
    <title></title>
    <link rel="stylesheet" type="text/css" href="../css/main.css">
</head>
<body>
    <div class="details-box warp">
        <div class="form-group clearfix">
            <label>电影名称</label>
            <input type="text" value="<%= data.name %>" id="name">
        </div>
        <div class="form-group clearfix">
            <label>作者</label>
            <input type="text" value="<%= data.author %>" id="author">
        </div>
        <div class="form-group clearfix">
            <label>电影描述</label>
            <textarea id="desc"><%= data.info %></textarea>
        </div>
        <div class="form-group clearfix">
            <label>地址</label>
            <input type="text" value="<%= data.src %>" id="addressSrc">
        </div>
        <div class="submit" id="submit"><%= data.name ? '更新' : '保存'%></div>
    </div>

    <input type="hidden" id="id" value="<%= data.id %>">
    <script type="text/javascript" src="http://code.jquery.com/jquery-1.9.1.min.js"></script>
    <script type="text/javascript">
        $(".submit").on("click",function(){
            var name = $("#name").val();
            var author = $("#author").val();
            var desc = $("#desc").val();
            var addressSrc = $("#addressSrc").val();
            var id = $("#id").val();
            $.ajax({
                url:'/addVideo',
                type:'post',
                data:{
                    id:id,
                    name:name,
                    author:author,
                    info:desc,
                    src:addressSrc
                },
                success:function(res){
                    alert(res.msg)
                }
            })
        })
    </script>
</body>
</html>